<h1><%=@task.title%></h1>

<p><%=@task.details%></p>

<p><%=@completed%></p>

<%= link_to 'Go back', tasks_path%> |
<%= link_to 'Edit', edit_task_path%> |
<%= link_to 'Delete',
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
